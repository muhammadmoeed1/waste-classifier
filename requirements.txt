# Core ML
tensorflow>=2.16
numpy
pillow
scikit-learn
matplotlib
opencv-python

# RAG (embeddings + vector search)
sentence-transformers
faiss-cpu

# Multi-item object detection (pretrained YOLOv8n)
ultralytics

# Model optimization (export/benchmark; see ml/export_onnx.py)
tf2onnx
onnxruntime

# API / backend
fastapi
uvicorn[standard]
python-multipart
pydantic
python-dotenv
slowapi

# Persistence (psycopg2-binary is only actually imported when DATABASE_URL
# points at Postgres, e.g. in production -- local/dev SQLite doesn't need it)
sqlmodel
psycopg2-binary

# GenAI
groq
